
  # Create Two Components

  This is a code bundle for Create Two Components. The original project is available at https://www.figma.com/design/uHungSQbNIHUxwX627oRel/Create-Two-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  